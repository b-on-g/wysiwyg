namespace $.$$ {
	/** User data stored in home land. Contains list of registry links. */
	export class $bog_wysiwyg_model_user_data extends $giper_baza_dict.with({
		Registries: $giper_baza_list_link,
	}) {}
}
