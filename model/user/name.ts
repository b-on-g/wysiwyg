namespace $.$$ {

	/**
	 * Имя автора в его домашнем ленде.
	 *
	 * Раньше комментарии читали имя через схему профиля из
	 * викторины. Одна строчка тащила в сборку весь Blitz (213 файлов), а вместе
	 * с ним его статический вызов `$bog_ui_router_path.activate()`, который
	 * подменял `$mol_state_arg` и ломал навигацию у всех, кто подключил
	 * редактор. На localhost тот роутер объявлен как no-op, поэтому вылезало
	 * это только на проде.
	 *
	 * Схема совместима по полю `Name`, так что уже записанные имена читаются.
	 */
	export class $bog_wysiwyg_model_user_name extends $giper_baza_dict.with({
		Name: $giper_baza_atom_text,
	}) {}

}
