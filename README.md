# bog/wysiwyg

Open-source блочный WYSIWYG-редактор на **$mol/MAM**. MIT License.

Часть проекта [WikiLive](../wiki/PRD.md) — вики-система с живыми таблицами.

## Запуск

```bash
# Внутри MAM workspace
# Dev server: http://localhost:9080/bog/wysiwyg/app/-/test.html
# Билд автоматический — не запускать npx mam вручную
```

## Горячие клавиши

| Клавиша                | Действие                         |
| ---------------------- | -------------------------------- |
| `Enter`                | ��овый блок                      |
| `Backspace` (пустой)   | Удалить блок                     |
| `/` (пустой блок)      | Slash-меню                       |
| `↑` / `↓` (в меню)     | Навигация по пу��ктам            |
| `Enter` (в меню)       | Выбрать пу��кт                   |
| `Escape` (в меню)      | Закрыть меню                     |
| `Ctrl/Cmd + B`         | **Жирный** (`$mol_hotkey`)       |
| `Ctrl/Cmd + I`         | _Курсив_ (`$mol_hotkey`)         |
| `Ctrl/Cmd + U`         | Подчёркнутый (`$mol_hotkey`)     |
| `Ctrl/Cmd + Shift + S` | ~~��ачёркнутый~~ (`$mol_hotkey`) |
| `Ctrl/Cmd + K`         | Вставить ссылку                  |
| `Ctrl/Cmd + J`         | AI-меню                          |
| `**text**`             | **Жирный** (markdown)            |
| `*text*`               | _Курсив_ (markdown)              |
| `` `text` ``           | `Код` (markdown)                 |
| `~~text~~`             | ~~Зачёркнутый~~ (markdown)       |
| `[text](url)`          | Ссылка (markdown)                |
| `[[page_id]]`          | Вики-ссылка (markdown)           |

## Типы блоков

Те��ст, Заголовки H1-H3, Код, Цитат��, Список, Разделитель, Картинка, Callout (плагин)

## Архитектура

Блочный редактор: каждый блок — `$mol_view` + `contenteditable`. Подробности в [AGENTS.md](AGENTS.md).

## Статус реализации

### Готово

- [x] Блочный редактор с contenteditable --------------------
- [x] Типы блоков: параграф, H1-H3, код, цитата, список, разделитель, картинка --------------------
- [x] Slash-меню (/) с навигацией стрелками и Enter --------------------
- [x] Горячие клавиши через `$mol_hotkey`: Bold, Italic, Underline, Strikethrough --------------------
- [x] Ctrl+K — вставка ссылок, Ctrl+J ( @ ) — AI-меню --------------------
- [x] Markdown-форматирование при наборе: `**bold**`, `*italic*`, `` `code` ``, `~~strike~~`, `[text](url)`, `[[page_id]]` -----------------
- [x] Enter/Backspace — управление блоками -----------------
- [x] Локализация (locale=en.json, locale=ru.json) -----------------
- [x] Giper Baza интеграция (persistence, автосохранение через CRDT) -----------------
- [x] Ссылки между страницами `[[page_id]]` + обратные ссылки (backlinks) -----------------
- [x] Совместная работа (Giper Baza CRDT, индикатор синхронизации, аватары) -----------------
- [x] Комментирование к блокам (потоки комментариев на каждый блок) -----------------
- [x] Версионирование / история правок (`land.fork()`, restore) -----------------
- [x] AI-подсказки (`$mol_github_model`: Дописать, Переписать, Перевести, Упростить) ну типа работает ------------------
- [x] Картинки (paste, drag&drop, URL через slash-меню) ---------------
- [x] Drag & drop блоков (перетаскивание за ручку) ---------------
- [x] Граф связей между страницами (force-directed canvas) -----------------
- [x] Плагины / расширяемость (реестр плагинов, пример: Callout)

## Лицензия

MIT
